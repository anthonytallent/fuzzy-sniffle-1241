<h1><%= @chef.name %>'s Ingredients List</h1>
  <section id="chefs_ingredients">
    <h3><%= @chef.name %>'s special ingredients:</h3>
    <% @chef.chefs_unique_ingredients.each do |ingredient| %>
    <p><%= ingredient.name %></p>
    <% end %>
  </section>